{
  "pluginAlias": "Govee",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Homebridge/HOOBS plugin to control Govee devices that are supported by the official Govee API.",
  "footerDisplay": "For help and support please visit our [GitHub Wiki](https://github.com/bwp91/homebridge-govee/wiki).",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "default": "Govee"
      },
      "apiKey": {
        "type": "string",
        "title": "Govee API Key",
        "description": "The API key linked to your Govee account. This is obtained through the Govee mobile app.",
        "required": true
      },
      "debug": {
        "title": "Debug Logging",
        "type": "boolean",
        "description": "If enabled, more information will be added to the Homebridge log.",
        "default": false
      },
      "debugReqRes": {
        "title": "Request & Response Logging",
        "type": "boolean",
        "description": "If enabled, HTTP messages will be added to the log. Not recommended for long-term use.",
        "default": false
      },
      "refreshTime": {
        "title": "Refresh Timer",
        "type": "integer",
        "description": "An interval (in seconds) in which your devices will refresh with Govee.",
        "default": 15,
        "minimum": 10,
        "maximum": 300
      },
      "disablePlugin": {
        "type": "boolean"
      }
    }
  },
  "layout": [
    {
      "type": "fieldset",
      "title": "Required Settings",
      "description": "These are the basic settings that are required for this plugin to work.",
      "items": [
        "apiKey"
      ]
    },
    {
      "type": "fieldset",
      "title": "Optional Settings",
      "description": "A variety of optional settings for general use of this plugin.",
      "expandable": true,
      "items": [
        "debug",
        "debugReqRes",
        "refreshTime"
      ]
    }
  ]
}
