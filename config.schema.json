{
  "pluginAlias": "Govee",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "<p align=\"center\"><img width=\"75%\" src=\"https://user-images.githubusercontent.com/43026681/93664399-944e7780-fa66-11ea-8c5f-21b98d4532ce.png\"></p><p align=\"center\">For help and support please visit our <a href=\"https://github.com/bwp91/homebridge-govee/wiki\">GitHub Wiki</a>. We hope you find this plugin useful!</p>",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "default": "Govee"
      },
      "apiKey": {
        "type": "string",
        "title": "Govee API Key",
        "description": "The API key linked to your Govee account. This is obtained through the Govee mobile app.",
        "required": true
      },
      "debug": {
        "title": "Debug Logging",
        "type": "boolean",
        "description": "If enabled, more information will be added to the Homebridge log.",
        "default": false
      },
      "debugReqRes": {
        "title": "Request & Response Logging",
        "type": "boolean",
        "description": "If enabled, HTTP messages will be added to the log. Not recommended for long-term use.",
        "default": false
      },
      "disablePlugin": {
        "title": "Disable Plugin",
        "type": "boolean",
        "description": "If checked, the plugin will remove all Govee accessories from Homebridge and not load the plugin on Homebridge restart.",
        "default": false
      },
      "refreshTime": {
        "title": "Refresh Timer",
        "type": "integer",
        "description": "An interval (in seconds) in which your devices will refresh with Govee.",
        "default": 15,
        "minimum": 10,
        "maximum": 300
      }
    }
  },
  "layout": [
    {
      "type": "fieldset",
      "title": "Required Settings",
      "description": "These are the basic settings that are required for this plugin to work.",
      "items": [
        "apiKey"
      ]
    },
    {
      "type": "fieldset",
      "title": "Optional Settings",
      "description": "A variety of optional settings for general use of this plugin.",
      "expandable": true,
      "items": [
        "debug",
        "debugReqRes",
        "disablePlugin",
        "refreshTime"
      ]
    }
  ]
}
